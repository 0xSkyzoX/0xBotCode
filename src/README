We support:
    Public Commands:
        -avatar
        -server
            !server -info
        -bot
            !help
            !bot -info
        -code
            !code -learn (js, py, ts, css....)
            !code -learn module (discord.js, node.js, react.js....)
            !code -add "TYPE OF CODE (ex: javascript)" [SEND CODE AFTER THIS CMD]
            (This Commands uses only in Special Code server and other specif servers...)
            !code -add "POST" (post message content ) img: or footer....
            !code -remove "POST" (MSG_ID) ! ONLY HIS MESSAGE ! MODERATOR ONLY CAN DELETE ANYPOST
            
